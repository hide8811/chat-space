.side
  .side__header
    .side__header--user-name
      = current_user.name
    .side__header--icon<>
      = link_to new_group_path, class: "icon" do
        = icon('fas', 'edit')
      = link_to edit_user_path(current_user), class: "icon" do
        = icon('fas', 'cog')
  .side__list
    - current_user.groups.each do |group|
      =link_to group_messages_path(group), class: "side__list--group-name" do
        = group.name
      .side__list--massege
        メッセージはまだありません